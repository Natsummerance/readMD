// Host no-op is intentional until ReadMD maps its own safe, reduced-motion
// particle field.  The upstream overlay remains responsible for all pet motion.
export const playVibeHearts = () => undefined
export function PetHeartField(_props: { petW: number; petH: number }) { return null }
