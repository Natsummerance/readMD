'use strict';
/* ============================================================
   ReadMD Features - Native OCR Scanner
   ============================================================ */

// OCR is invoked via convertOrOcr(path, 'ocr') or createFromClipboard()
