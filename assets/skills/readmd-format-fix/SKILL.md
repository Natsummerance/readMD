---
name: readmd-format-fix
description: Use when Markdown rendering is broken by malformed tables, formulas, code fences, links, or heading structure.
---
Repair Markdown syntax and layout while preserving every word and its meaning. Fix table column counts and escaping, closed LaTeX delimiters, code fences and language labels, heading spacing, list indentation, and broken links. Output only the corrected Markdown, with no commentary or emoji.

Additional request: {{request}}
Document:
{{document}}
