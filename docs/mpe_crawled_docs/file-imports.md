# 文件导入系统 (@import)

MPE 独有的模块化嵌入语法 `@import "filepath"`：

1. **导入 Markdown 章节 (分章拼装书籍)**:
   `@import "filepath"`

2. **导入 CSV 数据并自动转为表格**:
   `@import "filepath"`

3. **导入局部源码行范围与高亮**:
   `@import "filepath"`

4. **导入图表文件与 PDF 页面**:
   `@import "filepath"`
   `@import "filepath"`
