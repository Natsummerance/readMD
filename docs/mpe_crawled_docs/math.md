# 数学公式 (LaTeX Math)

MPE 内置 **KaTeX** 与 **MathJax (v2/v3)** 双渲染引擎：
- **行内公式**: `$...$` 或 `\(...\)`
- **块级公式**: `$...$`、`\(...\)` 或 ```math 代码块
- **引擎切换**: 设置 `$...$` 或 `\(...\)`
- **宏定义**: 支持通过 `$...$` 注入全局 `\(...\)` 学术简写宏
