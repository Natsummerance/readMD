# 桌宠与插件修复收尾（2026-09-06）

## Git 核对

恢复时 HEAD 为 `ce30757`，`main` 比本地 `origin/main` 多 20 个提交；已跟踪文件无未提交修改。旧修复已包含在 `92c71d9`（桌宠与插件）、`5077a25`（转换与索引）及后续国际化提交中。本轮不推送、不启动子代理、不重跑旧测试。

## 沿用的验证结果

用户确认旧会话的插件安装/执行/禁用/卸载全流程、Live2D、115 项测试、53 项复测和 98 文件 ZIP 验证通过。本轮沿用这些结果，不将其表述为当前重新测试结果。

旧任务记录也包含真实插件全流程成功、Electron Live2D 验证和相关复测成功。`.pytest-review-results.txt` 与 `ui-tests/pet-review-ui.log` 是修复过程中的失败记录，不是最终验收结果，保留以便追溯。`ui-tests/test-results-pet-video/.last-run.json` 记录录像用例通过。

桌面扩展产物保留在 `build/ReadMD-Desktop-Pet-review.zip`；本轮未重新构建或解包验证。

## 三项入口的代码验收

| 项目 | 已实现的行为 | 验收边界 |
| --- | --- | --- |
| 面板重构 | `assets/index.html` 包含预览与状态区、分组设置、底部安装/重置操作；`assets/style.css` 实现桌面双栏与窄屏单栏 | 代码结构已核对；用户视觉验收尚未确认 |
| Hermes 桌宠库 | 打开面板加载 `/api/pets`，选择角色写入 `/api/pets/active`，更新精灵图并重新发布运行配置；可导入 PNG/WebP | 本地角色库，不是在线商城；现有 `pet-runtime.spec.js` 虽标题包含 gallery selection，正文没有角色切换断言，不能作为该行为的实测证据 |
| 运行模式入口 | “运行位置”支持阅读器内和独立桌面；一键安装调用 `/api/pets/runtime/install`，启用独立桌面且运行时缺失时先补装；配置失败后读取实际状态恢复界面 | 前端将 Live2D 配置为独立桌面，后端拒绝启用阅读器内 Live2D；默认安装仅支持 Windows，并依赖随附 ZIP，缺包返回 `pet_plugin_bundle_missing` |

以上是源码链路核对，未替代用户对外观、角色库范围和模式交互的最终确认，也未新增运行验证。

## 本地证据整理

pytest 临时目录与 Playwright 录像/追踪结果通过 `.gitignore` 排除，文件仍留在原处。针对固定本机端口的临时录像脚本移至 `scratch/pet-review-2026-09-06/` 保存，不纳入自动测试或提交。

## 2026-09-07 自动任务收尾复核

- 恢复时 HEAD 为 `f450198`，工作区干净，`main` 比本地 `origin/main` 多 22 个提交。旧任务的整理提交已经完成，无待收纳的源码改动；未 fetch、未推送。
- `f450198` 已将旧版手选 ZIP 的安装按钮改为一键安装，本次修正上表的过时描述。安装从程序目录或源码 `build` 目录查找随附 ZIP，不表示在线下载桌宠库。
- 本轮沿 HTML/CSS、前端事件、HTTP 路由、后端配置与运行配置发布链路检查三项入口，未重新运行插件、Live2D、115 项测试、53 项复测或 ZIP 验证。只对本轮文档修改执行 `git diff --check`。
- 用户最终验收仍开放：面板外观与窄屏布局是否符合预期；本地 Hermes 角色库及 PNG/WebP 导入范围是否满足需求；阅读器内/独立桌面及 Live2D 自动转独立桌面的交互是否接受。现有记录不足以把这些标为用户已确认。
