# v2.2.5 交接记录

状态：代码完成、本地全量验证通过，等待推送与发布（push/tag/Release 需用户批准）。

## 范围

- M1 TXT 智能转 Markdown：`readmd_modules/txtmd.py` 识别标题层级、对齐表格、分点列表并生成目录锚点；打开 `.txt` 自动结构化渲染 + 转换入口输出智能 MD。
- M2 剪贴板 Ctrl+V：首页按 `Ctrl+V`（排除 input/textarea/CodeMirror 焦点）或「从剪贴板新建」按钮，解析剪贴板 Markdown 为可编辑虚拟文档（Ctrl+S 存盘，不弹另存为）；「从剪贴板获取对话」复用同一解析路径。
- M3 URL 导入全放开：`readmd_modules/web.py` 默认 `allow_private`（fetch_html / fetch_document / localize_images / redirect 同步放开），`_api_chat_import` 与 w-web 一致；修复 URL 导入绕过 `_module_ready` 懒加载门禁导致的首次同步挂起。
- M4 安装器自动匹配旧目录：`setup_app.py` `state['default_dir']` 用 `detect_install_dir()` 预填已装版本目录；`setup.html` 文案联动。
- M5 内置升级推送：启动静默 GET `https://api.github.com/repos/Natsummerance/readMD/releases/latest`，比较版本后 Toast「发现新版本 vX」+ 点击跳转 Release 页；4s 超时失败静默、进程内缓存、不上传任何用户数据。
- M6 收尾：`release.yml` / `release.py` / `ui-tests` 版本号全部 2.2.5；`release_notes.md`、`release_notes_macos.md` 重写；CONTEXT.md 更新。
- LaTeX 调研按计划推迟 v2.2.6（`docs/research/v2.2.5-markdown-latex.md` 仅为内部 API 草案，非承诺）。

## 当前已知提交（codex/v2.2.5，基于 origin/main = v2.2.4）

| 提交 | 内容 |
| --- | --- |
| `5973b01` | M0 版本号 2.2.5 |
| `ea1649f` | M1 TXT 智能转 MD |
| `def6f7a` | M3 URL 全放开 + chat_import 门禁修复 |
| `fd5d7e9` | M4 安装器自动匹配旧目录 |
| `77c197f` | M2 剪贴板 Ctrl+V |
| `c2b501e` | M5 内置升级推送 |
| `c6c2b8d` | chore: bump release config to v2.2.5 |

未跟踪（禁止提交）：`.token-optimizer/`、`IDEA.md`。

## 本地验证结果（2026-08-18，全绿）

- 单测：`readmd_api_test.py` 10、`readmd_chat_import_test.py` 10、`readmd_convert_test.py` 23、`readmd_export_test.py` 27、`readmd_fix_test.py` 37/0、`readmd_performance_test.py` 6、`readmd_txtmd_test.py` 13、`readmd_upgrade_test.py` 4、`readmd_web_test.py` 21、`installer_setup_test.py` 12 —— 全部 OK。
- Playwright `ui-tests`：11/11 通过（含安装器紧凑视口、chat import 剪贴板/文件/链接、module entry 懒加载、离线 Defuddle）。
- `node --check assets/app.js`：SYNTAX-OK。
- `python readmd.py --selftest`：PASSED（版本一致性 2.2.5、http server、单实例、导出/转换）。
- `python tools/privacy_scan.py`：PASSED（102 个跟踪文件，无退役标记/明文 Key）。
- 注意：Playwright 运行中 `[WebServer] ERROR:root:http error: /api/ai/prompts` 为测试预期内的连接中断日志（客户端主动断开），不影响结果。

## 发布步骤（需用户批准后执行）

1. 合并/推送 `codex/v2.2.5` 到 `main`，推送 `v2.2.5` 标签（`deploy.bat [--skip-tests] [--tag v2.2.5]` 自动处理；不创建 Release）。
2. CI 门禁：Windows/macOS 测试 → 打包 → `release` job 仅在 `v2.2.5` 标签触发，校验版本/架构/资源/隐私/SHA-256 后创建 Release（五个资产 + SHA256SUMS.txt）。
3. 检查 Release 恰好五个资产，下载后本地复核 `SHA256SUMS.txt`。
4. 发布成功后 `release.py --verify` / `--update` 维护本地记录。

## 已知限制

- Windows 与 macOS 安装包未签名；Release 说明已包含 SmartScreen/隐私设置指引与 SHA-256 校验方法。
- 升级推送依赖 GitHub API 可用性；离线或网络受限时静默跳过。
