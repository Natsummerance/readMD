# ReadMD v2.3.8 当前验证状态

> 本文记录当前隔离分支的可复验状态。它不是发布声明，也不替代目标平台的原生验收。

## 候选边界

- 分支：`codex/v2.3.8-qoder-handoff`
- 当前候选代码提交：`5c5bdfd`（文档收尾提交随后附加；此前接管 checkpoint：`44f7bf9`；基线：`e6e08ab`）
- 工作树：干净；尚未合并 `main`、打 tag 或发布。
- 测试数据和报告只使用临时目录；没有把凭据写入仓库或测试产物。

## 已执行门禁

| 门禁 | 结果 | 备注 |
| --- | --- | --- |
| Python 全量测试 | 885 passed, 1 skipped | 在隔离数据目录执行；唯一 skip 是当前 Windows 权限不允许创建 symlink 的导入器分支 |
| 桌面/移动 UI | 205 passed, 3 skipped | 当前 Chromium desktop + mobile 全量（含 Chart.js）；Firefox/WebKit 尚未纳入当前证据 |
| 真实文件沙盒冒烟 | passed | 使用 README 副本验证转换、Python 代码执行、EPUB、Vega-Lite SVG；沙盒已删除 |
| Batch A 回归 | 16 passed | 包含窄窗、编辑布局、保存状态、主题和 Chart.js 离线回归（desktop + mobile） |
| VSCode 扩展 | 8 passed | 仅编译/单元覆盖，尚未覆盖全部命令的 Extension Host 流程 |
| i18n | 46 locale / 1155 keys | key 与占位符结构通过；语义人工审校仍未完成 |
| Prompt 静态扫描 | passed | 未发现运行时 system prompt 重复来源 |
| 隐私扫描 | passed | 1420 个文件；未发现扫描规则命中的凭据/本机路径 |
| 版本同步 | passed | 资源 bundle 与源码版本一致；候选提交为 `8fa6e3f` |
| Vega-Lite HTTP 冒烟 | passed | 离线生成 SVG，未访问外网 |
| 图表能力快照 | passed | `GET /api/diagram/capabilities` 明确返回每个引擎的离线/在线/不可用状态 |
| 宠物 API 冒烟 | passed | 列表、确认门禁、非法 slug 均返回稳定结果 |
| `git diff --check` | passed | 当前差异无空白错误 |

## 本轮新增实现

1. Vega/Vega-Lite 通过固定脚本和 vendored bundle 在本地 Node 进程生成 SVG；浏览器 CSP 不需要开放 `unsafe-eval`。
2. 其他图表引擎继续走按需浏览器渲染；不具备本地运行时的 D2/PlantUML 明确返回不可用或在线渲染状态，不伪报成功。
3. 图表能力接口现在按安装实例探测静态资源、Node 和本地 PlantUML；PlantUML 远程加载失败时预览会回退到安全源码提示，不再显示破损图片。
4. 冻结构建只接受随包的 Node 运行时；开发机 PATH 中的 Node 不会把缺少运行时的正式包误报为 Vega 可用。
5. 增加 Hermes 兼容的本地宠物目录、受限导入/列表/删除/激活 API；导入仅接受 PNG/WebP、大小和路径均有限制。
6. 代码块 `output=true` 可将标准输出作为受控标记回写文档，并复用正常保存/历史路径。
7. Skill 导入与生成失败仅显示本地化文案，不再把后端错误码、原始异常或中文兜底写入工作台。

## 尚未闭环的发布阻断项

### 图表与离线能力

- 未提供 D2 本地运行时；D2 只能显示安全错误。
- PlantUML 默认仍是在线 URL；只有检测到本地 Java/JAR 才能离线。
- Chart.js 已接入独立代码块 dispatcher，并通过 Chromium desktop/mobile 离线 canvas 回归；当前尚无每个引擎在正式打包应用中的冷启动截图/视频证据。

### 桌宠

- `src/readmd_modules/pet/store.py` 只负责安全的本地资源目录和 API。
- Hermes 适配器是可选插件，尚未形成主应用内置的完整桌宠 UI/平台闭环。
- 没有当前提交在 macOS/Linux 原生透明窗口上的真实证据；不能宣称这些平台已验证。
- Cubism Core/模型再分发许可仍需在正式资产中逐项确认。

### 生态与本地化

- VSCode 仍有运行时中文消息和硬编码 workflow，尚未建立完整 `package.nls.*` 体系。
- VSCode 目前只有 8 个自动测试，未覆盖声明的全部命令、流式取消和真实 Core 生命周期。
- MCP 尚无 Claude、Cursor、Cline 的当前提交端到端连接证据。
- Playwright 当前仅有 Chromium 项目；Firefox/WebKit、200% 缩放和完整 RTL 证据尚缺。
- 46 个 locale 只完成结构门禁；外围 UI 的逐条语义审校和长文本人工验收未完成。
- 主要 HTTP 转换、OCR、网页、上游来源和 AI 历史路径已收敛为稳定 `error_code`；仍有旧的 pywebview 兼容桥接分支返回本地化错误文本，需要在发布前继续统一。

## 结论

当前分支适合作为继续修复和本地 Preview 的基础，但不是正式候选：仍存在 P1/P2 级功能与证据缺口。任何“全平台可用”“全部离线”“正式发布”表述都必须等上述阻断项有当前提交、真实环境和可追溯产物后再使用。

下一步应在本分支继续完成错误码收敛、Skill/宠物路径安全加固、真实文件转换与压力测试，并重新生成带 commit 和 SHA-256 的 Preview 证据包；在用户验收前不合并 `main` 或发布。

## 2026-09-02 当前复核增量

以下结果覆盖本文件前述历史记录，均在工作区当前候选上重新执行：

- 当前分支仍为 `codex/v2.3.8-qoder-handoff`；本轮修改尚未提交、尚未合并 `main`、尚未打 tag 或发布。
- `python -m pytest -q`：`888 passed, 1 skipped, 109 subtests passed`（约 5 分钟）；唯一 skip 为 Windows 无 symlink 创建权限。
- Chromium desktop/mobile 全量 UI：`207 passed, 3 skipped`；Firefox/WebKit、原生窗口和真实客户端证据仍缺失。
- VSCode 扩展 `npm run test`：编译通过，`8 passed`。
- 新增 MPE fence 属性解析（`cmd`、`hide`、`echo`、`output`，含 `cmd=python` 和 `cmd=false`）及 UI 回归：`6 passed`（desktop/mobile）。
- PlantUML：有显式本地 Java/JAR 时走无 shell 的本地 SVG；无本地运行时时仅返回联网状态；WSD 与 D2 明确 `diagram_engine_unavailable`，不再把它们伪装成 PlantUML 成功。
- 代码块失败输出现在只显示本地化稳定错误，不再将后端异常、路径或 stderr 原文写入文档预览。
- VSCode 清单已改用 `package.nls.json` / `package.nls.zh-cn.json`，桥接层改用稳定错误码；完整运行时消息本地化、22 命令 Extension Host、流式取消和真实 Core 生命周期仍未完成。

本轮仍不能关闭的阻断项：D2/WSD 无可再分发离线运行时、PlantUML 默认在线、Hermes 桌宠无主应用原生闭环和许可证明、VSCode/MCP 真实客户端证据、Firefox/WebKit/200%/完整 RTL 与 46 locale 语义审校、正式平台安装/冷启动/签名/公证，以及当前提交绑定的 Preview 重打包。因而结论仍为 **NO-GO**，以上数字不得解读为正式发布通过。

## 2026-09-02 提交后复核（覆盖前述旧数字）

- 代码复核提交：`4b72d5a3f4982b1808517e37af8d0cf5a4ecd342`；本节对应的文档收尾会产生一个新的文档提交，提交后以 `git rev-parse HEAD` 为最终候选标识。
- 工作区在代码提交后为干净状态；本轮仍未合并 `main`、创建 tag 或发布。
- Python 全量：`888 passed, 1 skipped, 109 subtests passed`。唯一 skip 是 Windows 环境不能创建 symlink 的显式导入器分支。
- Playwright 四项目首次全量：`414 passed, 9 skipped, 1 failed`；唯一失败是 WebKit 在 DPR=2 下对 Chart.js canvas 内部像素尺寸的过严断言。断言已改为校验 CSS 尺寸与 DPR 上界，并重新生成 `assets/readmd.boot.js`；涉及 MPE/图表的四项目定向回归为 `44 passed`。完整四项目全量需在该断言修复后再跑一次，未重跑前不得写成全量全绿。
- Chromium desktop/mobile 全量历史结果仍为 `207 passed, 3 skipped`；Firefox/WebKit、原生窗口和真实客户端仍不能以此替代平台证据。
- VSCode：编译和现有桥接/侧栏测试 `8 passed`；manifest 的 nls 占位符和稳定错误码已收口，但 22 命令 Extension Host、流式取消、完整 Core 生命周期和全 locale 本地化仍未完成。
- 静态门禁：版本同步、Prompt 来源、隐私扫描、46 locale 结构校验、`readmd.py --selftest`、`node --check assets/js/reader/render.js`、`git diff --check` 均已通过；这些结果不等于语义翻译、原生平台或发布签名通过。

本节之后仍保持 NO-GO。未关闭阻断项为：D2/WSD 无离线运行时、PlantUML 默认在线、Hermes 桌宠缺主应用原生闭环和再分发许可证据、VSCode/MCP 真实客户端证据、Firefox/WebKit 完整全量与 200%/RTL 证据、46 locale 语义审校、正式平台安装/冷启动/签名/公证、真实文件与压力测试，以及绑定最终提交的 Preview 重打包。

## 2026-09-02 四浏览器全量复核（最终覆盖）

- 运行时代码候选提交：`036a94c6d52b0a800650358de5d9a581cf855dc9`；本节随后仅有文档/证据更新，不改变运行时代码。工作区干净，仍未合并 `main`、打 tag 或发布。
- Playwright 全量（Chromium、Firefox、WebKit、mobile）：`415 passed, 9 skipped, 0 failed`（共 424 项，约 17.2 分钟）。9 个 skip 均为有意避免重复硬件/视口测量：`touch-targets` 在 mobile 单独执行 3 次，`startup-performance` 在 desktop 单独执行 3 次，`readmd-performance` 在 desktop 单独执行 3 次；不是失败或依赖跳过。
- WebKit Chart.js 断言已按 CSS 尺寸与 DPR 上界校验，修复了此前唯一的 1920×1080 backing-store 误报；MPE/图表定向回归仍为四项目 `44 passed`。
- Python 全量仍为 `888 passed, 1 skipped, 109 subtests passed`；唯一 skip 为 Windows 无 symlink 创建权限的导入器安全分支。
- 版本同步、Prompt 来源、隐私扫描、46 locale 结构校验、`readmd.py --selftest`、JS 语法检查和 `git diff --check` 均保持通过。

在最后一次仅清理 vendored 资产空白后，当前提交上的受影响四项目回归（`v238-batch-a-fixes.spec.js` + `rendered-buttons.spec.js`）再次为 `44 passed`；没有因 Mermaid 空白归一化引入行为回归。

该全量结果只证明当前源码在本机浏览器测试矩阵中的行为，不改变平台和发布结论。D2/WSD、默认在线 PlantUML、Hermes 原生桌宠闭环/许可证、VSCode/MCP 真实客户端、46 locale 语义审校、正式平台安装/冷启动/签名/公证、真实文件与压力测试、Preview 产物绑定等阻断项仍开放，因此当前候选依然 **NO-GO**。

## 2026-09-02 步骤1：图表引擎策略收口（工作区未提交）

- 入口收口：`assets/index.html` 图表插入模态框移除 D2 选项；所有选项标注可用性（PlantUML 标注"默认联网渲染，本地 Java+JAR 可离线"，其余标注"离线"）。`assets/js/editor/editor.js` 的 `DIAGRAM_SAMPLES` 同步移除 D2 样例。
- 宣传复核：README / README.en / release notes / VSCode README / 46 locale 文件中无对 D2、WSD 的可用性宣称（Phytium D2000 为芯片名误报），无需改动。
- 渲染路径保持不变：`assets/js/reader/render.js` 调度器保留 `d2`/`wsd`/`ditaa`，旧文档块仍路由到后端 `diagram_engine_unavailable` 安全报错；`GET /api/diagram/capabilities` 与 `tests/test_diagram_rendering.py`（断言 d2 available=False）不受影响。
- 静态门禁：`python tools/sync_version.py`（重打 bundle 2.3.8-preview.1）、`node --check assets/readmd.boot.js`、`git diff --check` 通过。
- 定向回归（desktop+mobile，Chromium）：`readmd-ui.spec.js -g "core workflow controls satisfy accessibility contracts"` = `2 passed`；`v238-batch-a-fixes.spec.js` + `rendered-buttons.spec.js` = `22 passed`；合计 `24 passed, 0 failed`。
- 本节为工作区增量，尚未提交；"D2/WSD 无离线运行时"按步骤1策略转为明确标注的不可用引擎（入口移除 + 能力接口如实上报），不再作为隐性行为差异。

## 2026-09-02 步骤2：Hermes 桌宠闭环（工作区未提交）

- 许可证逐项证据：`packages/readmd-hermes-pet-adapter/assets/hermes-sprite.png`（SHA-256 `e328d387a2fc…97a3`）与 NOTICE.md 登记逐字节一致；`assets/pet/model/` 6 个 Cubism 文件哈希与权利记录 `assets/pet/model/readmd.live2d.json` 逐项一致，`ARCH-CHAN-CC0-1.0.txt` 全文随仓分发；hermes 上游 MIT LICENSE 与锁定 revision（fb27614addac…）的 UPSTREAM.md 位于 `third_party/hermes-agent-pet/`；Cubism Core 不在主仓内，仅随可选插件 ZIP 分发，缓存文件 SHA-256 `25ae938cb4fe282ce189b357bcc97e603d1e1f7ec78bf04150d401c23cdc792f` 与 `packages/readmd-hermes-pet-adapter/scripts/build.mjs` pin 一致；发布登记记录 `accepted-2026-09-01-readmd-plugin-publisher` 在案；`verify_model_bundle` 返回 `ready:true / code=ready_for_platform_probe / license=CC0-1.0 / asset_count=6`。
- 构建与分发证据：adapter 构建 EXIT=0（vite 8.2.0，140 modules），stage EXIT=0，`scripts/verify-upstream.mjs` 三项 SHA 输出一致。staged 产物 `packages/readmd-hermes-pet-adapter/stage/readmd-pet-windows/` 磁盘共 99 个文件 = 清单 98 条 + 清单自身；`readmd-pet-plugin.json`（format_version 1、id `readmd-hermes-pet`、optional:true、upstream MIT@fb27614addac）的 98 条 path+sha256 与磁盘逐条比对全部匹配（bad: NONE）；`app/models/arch-chan/` 9 个文件齐全；`app/vendor/live2dcubismcore.min.js` 207,155 字节。
- 主应用 HTTP 探针（真实 `ReadMDHTTPServer(("127.0.0.1", 0), readmd.Handler)` + serve_forever 线程 + http.client 直连，patch `readmd.SETTINGS_FILE`/`DATA_DIR` 到隔离临时目录，不启动 UI）：14/14 断言通过（PROBE_EXIT=0）。负向契约：`GET /api/pets` GET-only（其余方法 405 method_not_allowed）、缺失 thumb 404 pet_not_found、导入/激活/移除无 confirm 均 400 confirmation_required、坏 base64 400 pet_spritesheet_format_invalid、未知 slug 激活 404 pet_not_found；正向闭环：导入真实 hermes-sprite（200，slug `probe-hermes`）→ 列表可见 → 激活 200 `active=probe-hermes` → 缩略图 200 `image/png` 904,171 字节 → 移除 200 → 列表恢复为空。
- Python pet 基线：pet 相关测试共 `64 passed`（1.74s）。
- 明确边界：macOS/Linux 原生透明窗口与真实拖拽/托盘交互证据无法在 Windows 本机产出，本节不宣称这些平台已验证，推迟到步骤6 平台实测。
- 本节为工作区增量，尚未提交；桌宠的许可证、构建分发、HTTP 契约证据已闭合，剩余开放项为原生平台交互与冷启动（步骤6）。

## 2026-09-02 步骤3：VSCode/MCP 流式取消与命令全覆盖（工作区未提交）

- MCP server（`packages/mcp-server/readmd_mcp_server.py`）流式取消架构：每个 tool call 由 `run_stdio_server` 主循环派发到独立工作线程执行；`handle_tool_call` 接受 `progress`/`cancel_event`；客户端发来 `notifications/cancelled`（`params.requestId`）时置位对应事件（:858-863）。`readmd_ai_chat` 分支为 drain 循环，采用"先检查取消再拉取"的顺序，并引入 `emitted` 计数保证取消前后消息计数一致（修复了此前 `index` NameError）；流式路径逐条回发 `notifications/progress`（含 `progressToken`、`progress`、`message`）。
- **关键 bug 修复（闭包绑定）**：`run_stdio_server` 内 `_run_call` 原先闭包引用 while 循环中被重绑定的局部变量（req_id/name/arguments/progress/cancel_event），高并发下请求会错拿下一次迭代的变量值。修复为默认参数快照：`def _run_call(req_id=req_id, name=name, arguments=arguments, progress=progress, cancel_event=cancel_event): ...`，线程只读取调用瞬间的值。该修复由 stdio 真实子进程测试验证。
- 动态 Skills：`readmd_ai_assistant` 返回 `workflow_id/workflow_name/skill_id/system_prompt/user_payload`（修复了 payload 键名 `system_prompt`），skill 列表随内置 Skill 动态生成。
- VSCode bridge（`src/bridge.ts`，编译 `out/bridge.js`）：`callMcpMethodInternal` 将 `onProgress` 映射为请求参数 `_meta.progressToken`，`consumeOutput` 消费 `notifications/progress` 并转发非空 message；CancellationToken 触发时 `settle(false, Error('ai_cancelled'))` 并向 server 写出 `notifications/cancelled`（requestId 与请求 id 一致，由测试断言）；`aiChatStreaming` 基于 `readmd_ai_chat` 工具封装，60s 超时沿用 `core_operation_timeout`。
- VSCode extension：AI 工作台接入流式 UI（onChunk 逐段渲染、取消按钮走 CancellationToken）；`setupMcpServer` 三种客户端配置（vscode / cursor / clipboard）落盘 `python readmd_mcp_server.py` + `PYTHONIOENCODING=utf-8` 的启动契约由测试逐字段断言。
- 测试结果（全部 EXIT=0）：① MCP Python 门禁 `pytest tests/test_mcp_stdio_runtime.py tests/test_mcp_server.py -p no:cacheprovider -q` = **25 passed（3.98s）**，stdio 真实子进程覆盖并发多请求、progress 转发、取消（≥3 条 progress 后取消，server 收到 notifications/cancelled 且后续不再产出）、未知方法 -32601、动态 skill payload；② 扩展 node 门禁 `node --test test/bridge.test.js test/sidebarProvider.test.js test/commands.test.js` = **25 passed（0.5s）**，覆盖 bridge 流式 progress/cancel 线协议、22 命令全覆盖注册+取消路径、setupMcp 配置格式、侧边栏 tree provider。
- 图表策略对齐：`src/extension.ts` `insertDiagram` 移除 `d2` 模板（与步骤1 主应用入口收口一致，其余 plantuml/tikz/wavedrom/vega-lite/graphviz/bitfield 保留），`npm run compile` EXIT=0 后复跑 node 门禁 25/25。
- 明确边界：Claude/Cursor/Cline 真实客户端交互式联调无法在本机（Windows、无对应客户端会话）产出，本轮以"线协议 + 配置文件契约 + 真实 stdio 子进程"三类自动化证据替代；真实客户端点对点验证推迟到步骤6/7。
- 本节为工作区增量，尚未提交；步骤3 的代码与自动化门禁已闭合，剩余开放项为真实客户端交互验证（步骤6/7）。

## 2026-09-02 Windows 11 验收轮复核与 PPTX/XLSX 修复（工作区未提交）

- 用户验收轮（Windows 11 真实环境）：真实文件副本（MD/TXT/HTML/CSV/JSON/XML/RTF/ODT/DOCX/PDF）、长文档（10 万/100 万字符）、20 文件混合批量 20/20、OCR（WinRT + PDF 文本层）、代码运行、网页提取、Skill 导入（目录+ZIP 含路径穿越拦截）、样式持久化/导出/EPUB、AI（本地 OpenAI 兼容 SSE、Bearer、Skill 注入、4 路并发）、API 6/6、UI 44/44（Chromium/Firefox/WebKit/mobile）、portable `--selftest` 均通过。产物 `dist/ReadMD-portable-v2.3.8-preview.1.exe`（约 150 MiB），SHA-256 `110c4cc8c6e9e02af92f57d66ee271c9930d43c3d944cea953d6ce6fe6b4a4`。用户确认 7 个问题并清理了全部 `readmd-prod-*/readmd-stress-*/readmd-api-*/readmd-pkg-*` 临时目录；用户结论：Windows 11 核心功能真实可用，但尚不能称为全功能正式候选，本轮未合并、未打 tag、未发布。
- **产物-代码时间线（关键分诊）**：portable exe 构建于 9 月 1 日 21:33，早于 9 月 2 日步骤 1-3 的全部工作区改动——本轮验收测的是步骤 1 之前的代码，因此"UI 仍见 D2""PlantUML 未标注联网"属预期（步骤 1 已在工作区修复）；工作区未提交文件实为 13 个（10 改 + 3 未跟踪：`tests/test_mcp_stdio_runtime.py`、`tests/test_v238_office_native.py`、`packages/vscode-extension/test/commands.test.js`），并非用户列出的 4 个；portable `--selftest` 跳过 `installer/setup_app.py not found` 为设计内行为（`readmd.py:4921-4933` 按执行文件旁目录查找，portable 构建刻意不含 installer/），非打包缺陷。
- **问题 1 修复（PPTX/XLSX `unsupported_format`）**：根因是 OOXML 包（zip）内含 NUL 字节，命中二进制兜底 `_BINARY_ONLY_EXTS or _looks_binary(path)` 后返回 `unsupported_format`（`src/readmd_modules/convert.py` convert_verbose 末段）。修复采用与 `_odt_to_md` 相同的纯 stdlib 轻量模式，零新增依赖、bundle 不变：`_xlsx_to_md`（sharedStrings + inline/bool/数值单元格、`r` 属性列号 A1→0、rels 定位 sheet、每 sheet 输出 MD 表格、`P|管` 转义）；`_pptx_to_md`（presentation.xml 的 sldIdLst 经 rels 定序，`ph` title/ctrTitle → `##` 标题、段落文本、`a:tbl` → MD 表、无标题时 `## Slide N` 兜底）；两者失败时回落 MarkItDown 再失败才报错。旧版二进制 `.xls/.ppt` 无内置解析器，仅在装有 MarkItDown 时可转换，否则报明确的 `legacy-office` 错误，不再静默。分发逻辑插入在二进制守卫之前；`convert_verbose` docstring 的 engine 枚举同步补 `xlsx/pptx`。
- 测试过程中暴露并修复一个真实 bug：`_ooxml_rels` 最初用 base_dir 拼接 rels 路径（`xl/_rels/xl.xml.rels`），实际 rels 文件名跟随主文件名（`xl/_rels/workbook.xml.rels`、`ppt/_rels/presentation.xml.rels`）；xlsx 无 fallback 掩盖，首跑 xlsx 用例即失败，已加 `base_name` 参数修复。pptx 用例此前通过是因 slide 序号 fallback 与真实顺序重合，不能作为 rels 路径正确性证据。
- 宣传面复核：`readmd.py:121` CONVERT_EXTS、文件选择器 `*.pptx;*.xlsx`、`src/readmd_core/dialogs.py` 过滤器、`packages/mcp-server/readmd_mcp_server.py` 工具描述原本就宣称支持 pptx/xlsx——本修复使这些宣称变为真实，无需改动宣传文案；主前端 bundle 无 pptx/xlsx 独立页签（bo_p2/lo_partC 中的 locale 键属遗留面，未被主应用消费）。
- 门禁（全部 EXIT=0，解释器为 PATH `python`，pytest 9.1.1；注意仓库 `.venv` 为空壳仅含 pip/setuptools，不可用于跑测试）：新套件 `tests/test_v238_office_native.py`（5 用例：列号、xlsx 表格、pptx 幻灯片、坏 zip 拒绝、legacy 稳定错误）+ 转换回归 6 文件 = **107 passed**；`readmd.py --selftest` PASSED；`git diff --check` 干净（仅 CRLF 提示）。全量 tests/ 中无任何既有断言依赖 pptx/xlsx 的旧 `unsupported_format` 行为。
- 问题 2-4 现状：PlantUML 离线性与 D2/WSD 已由步骤 1 按既定策略收口（入口移除/标注），待新产物复验；OpenRouter 无 `OPENROUTER_API_KEY` 环境不可测，跨平台原生证据无目标平台——两者按用户框架推迟到步骤 6/7。
- 结论维持 **NO-GO**：当前 dist 产物过期（早于步骤 1-3 与本修复），重新打包后需按 commit + SHA-256 重新验收；后续仍开放步骤 4（46 locale 语义审校 + 无障碍验收）、步骤 6（多平台原生 Preview + 真实客户端 MCP 联调）、步骤 7（用户最终验收后才 merge main / tag v2.3.8 / Release）。
