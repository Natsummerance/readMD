# SQLite WAL 双向链接索引系统

返回架构总览：[[01-System-Overview]] 与 [[02-Architecture]]。

## 存储结构
- `documents` 表：存储文档元数据（路径、修改时间、文件大小、标题）。
- `links` 表：存储全部正向与反向链接拓扑（来源路径、目标名称、行号、别名、是否死链）。

支持单次毫秒级增量更新，文件未修改时直接跳过，零重复开销。
