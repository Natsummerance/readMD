## 下载

> 安装包（推荐）：下载 **ReadMDSetup-v2.1.1.exe**，双击即可安装（内含动画安装界面，支持设为 .md 默认打开方式，可随时在「设置 → 应用」中卸载）。
> 便携版：**ReadMD-portable-v2.1.1.exe** 免安装，双击直接运行。
> Win7 用户：下载 **ReadMDSetup-2.1.1-Beta-win7-x64.exe**（v2.1.1 Beta 兼容版，详见下方说明）。

## Win7 兼容版（v2.1.1 Beta）

- **适用**：Windows 7 SP1 x64（正式版 v2.1.1 基于 Python 3.10 + pywebview 6.x，不支持 Win7）。安装包内嵌固定版 **WebView2 109 运行时**（Win7 最后支持线），安装时自动放入安装目录，无需系统级 WebView2；Win7 且系统未装 WebView2 时安装界面默认勾选「安装 WebView2 运行时」
- **保留**：秒开（onedir 目录安装 + 单实例托盘）、三主题阅读 / 自动修正 / 目录 / 搜索 / 编辑 / 导出 PDF·DOCX·HTML / 打印、docx / pdf 转 Markdown（自动保存 + 批量 + 严格校验）
- **裁剪**：OCR（WinRT 仅 Win10+）、AI 助手、网页转 MD、docx / pdf 以外的格式转换在 Win7 版入口明确提示「暂不支持」
- **版本**：仅 Win7 版使用版本串 `2.1.1 Beta`；正式版 / 便携版保持 `2.1.1`；独立 pre-release 发布，不影响正式版
- **前置条件**：Win7 SP1 x64、.NET Framework 4.8、VC++ 2015-2022 运行库（x64）

## 新增（v2.1.1）

- **AI 助手连接自定义**：AI 面板新增「连接设置」卡片——提供商预设可选但 API Key 必填（本地 Ollama 除外）、Base URL 可编辑并保存为该提供商的自定义覆盖（可一键恢复默认）、支持 OpenAI `chat/completions` / `completions` / `responses` 与 Anthropic `messages` 四种响应方式；「获取模型」一键通过 Key 拉取可用模型列表（失败可手动输入模型名）；对话实时监控 token 用量（本次 prompt/completion/total + 会话累计），随会话保存
- **编辑实时预览**：编辑工具栏新增「无 / 左 / 右 / 下 / 上」五档预览布局（默认无，与旧版一致），300ms 防抖实时渲染、跟随三主题；可开「滚动同步」按比例双向联动；转换 / OCR / 网页结果也可一键进入编辑（保存走另存为并切换为文件模式）
- **转 MD 自动保存 + 批量 + 质量升级**：单文件与批量转换结果自动保存到源文件同目录同名 `.md`（report.docx → report.md）并直接以可编辑文件打开；「转 Markdown」面板支持多选文件 / 整文件夹（递归 ≤200 个）批量转换、实时进度、覆盖开关、打开结果目录；docx 专用解析（OMML 公式转 LaTeX、标题层级、表格、等宽代码块、样式级列表），pdf 专用解析（表格还原 + 公式启发式），其余格式 MarkItDown 兜底；输出经严格校验（代码围栏 / 表格 / 公式定界符 / 替换符 / 图片引用），安全项自动修复并给出质量警告
- **修复**：转换结果此前「只能看不能存/不能编辑」的问题已修复；AI 会话模型改为可输入框 + datalist，兼容任意模型名

## 新增（v2.1.0）

- **一键导出 PDF / DOCX / HTML**：工具栏「导出」按钮（原打印按钮）打开导出面板，选格式与样式即可导出当前文档（含未保存的编辑、转换 / OCR / 网页结果）；导出后可直接「打开」或「所在文件夹」
- **可视化样式定制**：内置「简约 / 经典 / 商务」三套预设；全量可调——纸张大小 / 方向 / 页边距、封面与目录、正文与各级标题（颜色 / 字号 / 加粗 / 对齐）、表格（表头颜色 / 边框 / 斑马纹 / 单元格字号与内边距）、代码块、引用、链接、页脚页码、PDF 元数据；自定义样式可「存为预设」复用，参数自动记忆
- **公式本地渲染**：`$...$` / `$$...$$` 公式在 PDF / DOCX 中由内置引擎渲染为图片（离线、无需安装 LaTeX，含中文公式）；HTML 导出保留 MathJax 完整渲染
- **图片与排版**：本地图片按文档目录解析并嵌入 PDF / DOCX（缺失自动跳过并提示）；PDF 支持封面 / 目录页 / 书签 / 页码；DOCX 标题使用 Word 内置 Heading 样式（导航窗格直达）；HTML 为单文件（内联 marked + MathJax），亮 / 暗 / 米色三主题，任何浏览器离线打开

## 修复（v2.0.1）

- **修复安装器黑屏弹窗**：v2.0.0 安装包 / 卸载器使用 PyInstaller `--splash` 启动画面，但代码未调用 `pyi_splash.close()`，导致黑色启动画面永远置顶、无法关闭，在低配机上会挡住安装界面，表现为「安装完退不掉、软件打不开」。v2.0.1 彻底移除启动画面，并在安装器代码中加入防御性关闭逻辑
- **版本号统一**：安装器与主程序版本统一为 2.0.1，安装 / 升级后注册表与 `install.json` 均为最新版本，避免版本显示不一致

## 新增（v2.0.0）

- **秒开启动**：安装版改为目录安装（onedir），彻底消除单文件 96MB 每次启动解压的 6 秒等待——冷启动窗口可用 ≤1.5s，低配机 / 机械硬盘 ≤2s
- **单实例常驻**：关闭窗口隐藏到系统托盘，再双击 .md 瞬时唤起（<0.3s）；托盘菜单「显示 / 打开文件 / 退出」
- **界面全面改版**：现代清爽阅读风——44px 工具条 + 内联 SVG 图标、「更多」二级菜单、欢迎页最近文件网格、浅色 / 暗色 / sepia 三主题全套设计 token
- **阅读体验**：标题层级、代码块（语言标签 + 圆角）、表格、引用、任务列表排版精修；目录滚动高亮 + 层级缩进；大文档骨架屏
- **无障碍**：可见焦点环、对比度 ≥4.5:1、动画遵循系统「减弱动态效果」、图标按钮 aria-label

## 完整功能

- 秒开渲染：先渲染 Markdown，后台懒加载转换 / OCR / 网页 / AI 模块
- 自动修正：表格、加粗、公式、标题等常见错误（只影响显示，不改原文件）
- AI 助手：15+ 提供商预设 + 自定义，快速阅读 / 修改 / 扩充 / 续写 / 润色，Prompt 模板，历史会话
- 万物转 MD：docx / pptx / xlsx / pdf / html 等（MarkItDown）
- 扫描转 MD：Windows 内置 OCR（离线、支持中文）
- 网页转 MD：URL 抓取正文，可批量爬取
- 主动编辑：CodeMirror 6 语法高亮 + 自动补全 + 工具栏插入，Ctrl+S 保存
- 导出：PDF / DOCX / HTML，样式预设 + 全量定制，公式本地渲染
- 移动端共享：手机扫码在同一 Wi-Fi 阅读
- 大文档增量流式渲染、三主题、目录、搜索、打印

## 校验

| 文件 | SHA256 |
| --- | --- |
| ReadMDSetup-v2.1.0.exe | `246819FA66449EC2B2F1FA54F376B099A3A51DEA4AD050CF4170EE0703C4B442` |
| ReadMD-portable-v2.1.0.exe | `E5424C4B23F70E6D2772259BD18E744449A8D42D8AA50571E5415D8181128713` |
